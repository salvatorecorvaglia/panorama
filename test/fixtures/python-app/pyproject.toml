[project]
name = "python-fixture"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.28",
  "httpx[http2]==0.27.0",
  "tomli; python_version < '3.11'",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.5"]

[dependency-groups]
test = ["coverage>=7.0"]
